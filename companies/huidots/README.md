# HuiDots company package (Revision 2)

Portable `agentcompanies/v1` package for the HuiDots org.

Apply against the live HuiDots instance with:

```powershell
node patches/huidots-revision-2/apply-revision-2.mjs --api-base http://127.0.0.1:3100
```

See `patches/huidots-revision-2/README.md`.
